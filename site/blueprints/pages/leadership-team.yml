title: Leadership Team
icon: users

status:
  draft:
    label: Draft
    text: The volume is in draft mode and only visible for logged in editors
  unlisted:
    label: Public
    text: The volume is visible to the public

columns:
  - width: 1/1
    fields:
      volume:
        type: number
        step: 1
        width: 1/4
  - width: 1/2
    fields:
      coordinating_editors:
        label: Coordinating Editors
        type: pages
        query: site.find("contributors")
      publishers:
        label: Publishers
        type: pages
        query: site.find("contributors")
  - width: 1/2
    fields:
      archivists:
        label: Archivists
        type: pages
        query: site.find("contributors")
      graphic_design_liaisons:
        label: Graphic Design Liaisons
        type: pages
        query: site.find("contributors")
      web_editors:
        label: Web Editors
        type: pages
        query: site.find("contributors")
